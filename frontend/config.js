(function(window){
  // Default runtime config. This file can be generated from .env using the
  // generate-config.js script (npm run gen-config).
  window.__ENV = window.__ENV || {};
  window.__ENV.BASE_URL = window.__ENV.BASE_URL || 'telecom-icvr.vercel.app';
})(window);
